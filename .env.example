PORT=5000
NODE_ENV=development
JWT_SECRET=supersecretjwtkeyforadminpanel
ADMIN_EMAIL=admin@logixmart.com
ADMIN_PASSWORD=LogixmartAdmin2026!
# Used only to bootstrap the first SUPER_ADMIN on seed
ADMIN_NAME=Super Admin
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/logixmart?schema=public
API_BASE_URL=http://localhost:5000
# Must match the host:port browsers use to reach this API (used for /uploads public URLs).
# If PORT differs locally (e.g. 5001), set API_BASE_URL accordingly.
UPLOAD_DIR=uploads
